interface EmptyPart { name: string; }

const theWorld = () => console.log();

function theTesting(params: number) {
    if (true) {
        console.log("baba");
    }
    const superWorld = "kdjkf";
    console.log({ superWorld });
    return "name";
}
